var Trie = function(strValue){
	this.value = strValue ? strValue : '';
	this.children = [];
};

Trie.prototype.insert = function(){

};

Trie.prototype.contains = function(){
	
};

Trie.prototype.remove = function(){
	
};

Trie.prototype.getNexLetters = function(strValue){
	
};